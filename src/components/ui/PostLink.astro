---
import { getEntry } from 'astro:content';
import { renderTitle } from '../../lib/renderTitle';
const { id } = Astro.props;
const post = await getEntry('posts', id);
---

{post && <a href={`/publications/${post.id}`} set:html={renderTitle(post.data.title)} />}
